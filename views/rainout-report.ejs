<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Closure Report</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 1200px; margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em; }
        form { margin-bottom: 2em; padding: 1.5em; border: 1px solid #ddd; border-radius: 4px; background-color: #fafafa; }
        .form-grid { display: grid; grid-template-columns: 2fr 1fr; gap: 1em; align-items: end; }
        .form-grid label { display: block; margin-bottom: 5px; font-weight: bold; }
        .form-grid input[type="date"] { width: 100%; padding: 10px; box-sizing: border-box; border-radius: 4px; border: 1px solid #ccc; }
        button { padding: 10px 20px; background-color: #007bff; color: white; border: none; cursor: pointer; border-radius: 4px; font-size: 1em; font-weight: bold; }
        .chart-container { 
            margin-top: 2em;
            border: 1px solid #ddd; 
            padding: 1.5em;
            background: #fff;
            border-radius: 8px;
            min-height: 500px; 
            position: relative;
        }
        .error { color: #d9534f; font-weight: bold; background-color: #f2dede; padding: 1em; border-radius: 4px; margin-bottom: 15px; }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h1>Park Closure "Rainout" Report</h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <form action="/reports/rainouts" method="POST">
            <div class="form-grid">
                <div>
                    <label for="selected_date">Select a Date in the Desired Year:</label>
                    <input type="date" id="selected_date" name="selected_date" value="<%= selected_date %>" required>
                </div>
                <div style="align-self: end;">
                    <button type="submit">Generate Report</button>
                </div>
            </div>
        </form>

        <% if (report_data) { %>
            <div class="chart-container">
                <canvas id="rainoutChart"></canvas>
            </div>

            <script>
                // Pass EJS data to JavaScript
                const chartLabels = <%- JSON.stringify(report_data.labels) %>;
                const chartCounts = <%- JSON.stringify(report_data.counts) %>;

                const ctx = document.getElementById('rainoutChart').getContext('2d');
                new Chart(ctx, {
                    type: 'bar',
                    data: {
                        labels: chartLabels,
                        datasets: [{
                            label: 'Total Park Closures',
                            data: chartCounts,
                            backgroundColor: 'rgba(23, 162, 184, 0.7)',
                            borderColor: 'rgba(23, 162, 184, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: {
                            y: {
                                beginAtZero: true,
                                title: { display: true, text: 'Number of Closures' },
                                ticks: {
                                    stepSize: 1 // Ensure Y-axis goes up in whole numbers
                                }
                            },
                            x: {
                                title: { display: true, text: 'Month' }
                            }
                        },
                        plugins: {
                            title: {
                                display: true,
                                text: '<%= chartTitle %>',
                                font: { size: 18 }
                            },
                            legend: {
                                display: false
                            },
                            tooltip: {
                                callbacks: {
                                    label: function(context) { return `Closures: ${context.parsed.y}`; }
                                }
                            }
                        }
                    }
                });
            </script>
        <% } else if (typeof report_data !== 'undefined' && report_data === null) { %>
            <%# This block won't show on initial load, only after a query with no results %>
            <p style="text-align: center; font-weight: bold; margin-top: 2em;">No park closure data found for the selected year.</p>
        <% } %>

    </div>
</body>
</html>