<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Member Dashboard</title>
    <style>
        /* ... (all <style> content is unchanged) ... */
        body { font-family: sans-serif;
margin: 0; background-color: #f4f4f9; color: #333; }
        .dashboard-container { padding: 2em; max-width: 900px;
margin: 0 auto; }
        h1 { color: #444;
}
        .welcome-message { font-size: 1.2em; margin-bottom: 1.5em; padding-bottom: 0.5em;
border-bottom: 1px solid #ddd;}

        .renewal-banner {
            padding: 1em 1.5em;
            margin-bottom: 1.5em;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border: 1px solid transparent;
        }
        .renewal-banner.expiring {
            background-color: #fff3cd;
            border-color: #ffeeba;
            color: #856404;
        }
        .renewal-banner.expired {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
        }
        .renewal-banner p {
            margin: 0;
            font-weight: bold;
        }
        .renewal-banner .controls {
            display: flex;
            align-items: center;
            gap: 1em;
        }
        .renewal-banner .renew-btn {
            padding: 8px 12px;
            border-radius: 4px;
            text-decoration: none;
            font-weight: bold;
            color: white;
            white-space: nowrap;
        }
        .renewal-banner.expiring .renew-btn {
            background-color: #ffc107;
            color: #333;
        }
        .renewal-banner.expired .renew-btn {
            background-color: #dc3545;
        }
        .renewal-banner .close-btn {
            font-family: monospace;
            font-size: 1.5em;
            text-decoration: none;
            cursor: pointer;
            color: inherit;
            opacity: 0.7;
        }
        .renewal-banner .close-btn:hover {
            opacity: 1;
        }
        
        .profile-container {
            background: white;
padding: 1em 2em 2em 2em; /* More padding on top/bottom */
            border-radius: 8px;
box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        
        .profile-container h3 {
            margin-top: 1.5em;
border-bottom: 1px solid #eee;
            padding-bottom: 10px;
            font-size: 1.3em;
            color: #17a2b8;
}
        .profile-container h3:first-child {
            margin-top: 0;
/* No top margin for the first heading */
        }
        
        .info-pair {
            display: flex;
justify-content: space-between; /* Pushes label and value apart */
            align-items: center;
font-size: 1.1em;
            padding: 1em 0.5em; /* Padding top/bottom, slight left/right */
            border-bottom: 1px solid #f0f0f0;
}
        .info-pair:last-child {
            border-bottom: none;
/* No border on the last item */
        }

        .info-pair strong { 
            color: #333;
margin-right: 2em; /* Space between label and value */
        }
        .info-pair span { 
            color: #555;
/* Long text (like email) will no longer overflow */
        }
        
        .info-pair .status-Active { 
            color: #28a745;
font-weight: bold; 
        }
        .info-pair .status-Expired { 
            color: #dc3545;
font-weight: bold; 
        }       

    </style>
</head>
<body>
    <%- include('partials/member-header') %>
    <div class="dashboard-container">
        <% if (showRenewalBanner) { %>
            <% 
                let bannerClass = 'expiring';
                let bannerText = `Your membership expires on ${new Date(member.endDate).toLocaleDateString()}!`;
                if (member.status === 'Expired') {
                    bannerClass = 'expired';
                    bannerText = 'Your membership has expired!';
                }
            %>
            <div class="renewal-banner <%= bannerClass %>" id="renewalBanner">
                <p><%= bannerText %></p>
                <div class="controls">
                    <% if (hasPaymentMethods) { %>
                        <a href="/member/renew" class="renew-btn">Renew Now</a>
                    <% } else { %>
                        <a href="/member/manage" class="renew-btn">Add Payment to Renew</a>
                    <% } %>
                    <a class="close-btn" onclick="document.getElementById('renewalBanner').style.display='none'">&times;</a>
                </div>
            </div>
        <% } %>

        <h1>Member Dashboard</h1>

        <p class="welcome-message">
            Hello, <strong><%= member.firstName %> <%= member.lastName %></strong>!
</p>

        <div class="profile-container">
            
            <h3>My Membership</h3>
            <div class="info-pair">
                <strong>Member ID:</strong>
                <span><%= member.id %></span>
            </div>
      
      <div class="info-pair">
                <strong>Membership Type:</strong>
                <span><%= member.typeName %></span>
            </div>
            <div class="info-pair">
                <strong>Status:</strong>
                <span 
class="status-<%= member.status %>"><%= member.status %></span>
            </div>
            <div class="info-pair">
                <strong>Valid Through:</strong>
                <span><%= new Date(member.endDate).toLocaleDateString() %></span>
            </div>

            <h3>My Contact Info</h3>
         
   <div class="info-pair">
                <strong>Email:</strong>
                <span><%= member.email %></span>
            </div>
            <div class="info-pair">
                <strong>Phone:</strong>
                <span><%= member.phone ||
'N/A' %></span>
            </div>
             <div class="info-pair">
                <strong>Date of Birth:</strong>
                <span><%= new Date(member.dob).toLocaleDateString() %></span>
            </div>
        </div>
    </div>
</body>
</html>