<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Membership Purchase History</title>
    <%# --- Import Fonts --- %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --accent: #17a2b8;
            --dark: #343a40;
        }
        body { font-family: 'Poppins', sans-serif; margin: 0; background-color: #f0f2f5; color: #333; }
        
        .container { max-width: 1000px; margin: 2em auto; padding: 0 1em; }
        
        h1 { 
            font-family: 'Lobster', cursive;
            color: var(--dark);
            border-bottom: 2px solid #e9ecef;
            padding-bottom: 0.5em;
            margin-bottom: 1em;
            font-size: 2.5em;
        }

        .back-link { 
            display: inline-block; margin-bottom: 2em; 
            text-decoration: none; color: var(--accent); font-weight: 600;
        }
        .back-link:hover { text-decoration: underline; }

        .history-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.05);
            overflow: hidden; /* For table corners */
        }

        table { width: 100%; border-collapse: collapse; }
        
        th { 
            background-color: var(--dark); 
            color: white; 
            padding: 15px; 
            text-align: left; 
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.9em;
            letter-spacing: 0.5px;
        }
        
        td { 
            padding: 15px; 
            border-bottom: 1px solid #f0f0f0;
            color: #555;
        }
        
        tr:last-child td { border-bottom: none; }
        tr:hover { background-color: #f9f9f9; }

        .price-tag { font-weight: 700; color: var(--dark); }
        
        .action-btn { 
            display: inline-block;
            padding: 6px 12px;
            background-color: var(--accent);
            color: white; 
            text-decoration: none; 
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 600;
            transition: background-color 0.2s;
        }
        .action-btn:hover { background-color: #138496; }

        .empty-state { text-align: center; padding: 3em; color: #888; font-style: italic; }
    </style>
</head>
<body>
    <%- include('partials/member-header') %>

    <div class="container">
        <h1>Membership Purchase History</h1>
        <a href="/member/manage" class="back-link">&laquo; Back to Account</a>

        <div class="history-card">
            <table>
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Item</th>
                        <th>Term</th>
                        <th>Total</th>
                        <th>Receipt</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (purchases.length === 0) { %>
                        <tr>
                            <td colspan="5" class="empty-state">No purchase records found.</td>
                        </tr>
                    <% } %>
                    
                    <% purchases.forEach(purchase => { %>
                        <tr>
                            <td>
                                <strong><%= new Date(purchase.purchase_date).toLocaleDateString() %></strong><br>
                                <span style="font-size: 0.85em; color: #888;"><%= new Date(purchase.purchase_date).toLocaleTimeString() %></span>
                            </td>
                            <td><%= purchase.type_name %></td>
                            <td>
                                <%= new Date(purchase.purchased_start_date).toLocaleDateString() %> - 
                                <%= new Date(purchase.purchased_end_date).toLocaleDateString() %>
                            </td>
                            <td class="price-tag">$<%= parseFloat(purchase.price_paid).toFixed(2) %></td>
                            <td>
                                <a href="/member/purchases/receipt/<%= purchase.public_purchase_id %>" class="action-btn">View</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>