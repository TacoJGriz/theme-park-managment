<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Purchase Successful!</title>
    <style>
        body {
            font-family: sans-serif;
 margin: 0;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 2em;
 }
        .container {
            max-width: 500px;
 width: 100%;
            background: white;
            padding: 2.5em;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            text-align: center;
 }
        h1 {
            color: #28a745;
 /* Green for success */
            margin-top: 0;
            margin-bottom: 0.5em;
 }
        .receipt {
            background: #f8f9fa;
 border: 1px solid #dee2e6;
            padding: 1.5em;
            border-radius: 8px;
            text-align: left;
            margin-top: 2em;
            margin-bottom: 2em;
 }
        .receipt-item {
            display: flex;
 justify-content: space-between;
            padding: 0.5em 0;
            border-bottom: 1px solid #eee;
        }
        .receipt-item:last-child {
            border-bottom: none;
 }
        .receipt-item strong {
            color: #555;
 }
        .receipt-total {
            font-size: 1.2em;
 font-weight: bold;
            color: #000;
            margin-top: 0.5em;
        }
        .continue-button {
            display: block;
 width: 100%;
            box-sizing: border-box;
            padding: 12px 20px;
            background-color: #17a2b8;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1.1em;
 font-weight: bold;
            text-decoration: none;
            text-align: center;
        }
        .continue-button:hover {
            background-color: #138496;
 }
    </style>
</head>
<body>
    
    <div class="container">
        <h1>Thank You For Your Purchase!</h1>
        <p>Your membership is now active and your account has been created.
 A copy of this receipt has been saved to your account.</p>

        <div class="receipt">
            <h3>Purchase Confirmation</h3>

            <div class="receipt-item">
                <strong>Purchase ID:</strong>
                <span class="value"><%= receipt.purchaseId.substring(0, 8).toUpperCase() %></span>
            </div>
            
            <div class="receipt-item">
                <strong>Primary Member:</strong>
                <span><%= receipt.memberName %></span>
        
     </div>
            <% if (receipt.subMembers && receipt.subMembers.length > 0) { %>
                <div class="receipt-item">
                    <strong>Group Members:</strong>
                    <span><%= receipt.subMembers.length %> Added</span>
               
  </div>
                <% receipt.subMembers.forEach(sub => { %>
                <div class="receipt-item" style="padding-left: 2em;">
                    <span>- <%= sub.firstName %> <%= sub.lastName %></span>
                </div>
                <% });
 %>
            <% } %>
            <div class="receipt-item">
                <strong>Membership ID:</strong>
                <%# Show clean 8-char public ID %>
                <span><%= receipt.membershipId.substring(0, 8).toUpperCase() %></span>
            </div>
            <div class="receipt-item">
             
     <strong>Membership Type:</strong>
                <span><%= receipt.typeName %></span>
            </div>
            <div class="receipt-item">
                <strong>Valid Through:</strong>
                <span><%= receipt.endDate %></span>
            </div>
      
       
            <div class="receipt-item">
                <strong>Payment Method:</strong>
                <span><%= receipt.paymentMethod %></span>
            </div>
            <div class="receipt-item receipt-total">
                <strong>Amount Paid:</strong>
   
               <span>$<%= receipt.pricePaid.toFixed(2) %></span>
            </div>
        </div>

        <a href="/member/dashboard" class="continue-button">Continue to Your Account</a>
    </div>

</body>
</html>