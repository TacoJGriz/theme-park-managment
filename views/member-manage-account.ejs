<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Manage Account</title>
    <style>
        body { font-family: sans-serif;
margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 900px;
margin: 2em auto; }
        h1 { color: #444;
}
        .card {
            background: white;
padding: 2em;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 2em;
}
        .card h3 {
            margin-top: 0;
border-bottom: 1px solid #eee;
            padding-bottom: 10px;
            font-size: 1.3em;
            color: #17a2b8;
}
        
        /* --- NEW: Style for Edit link --- */
        .card h3 a {
            font-size: 0.7em;
            font-weight: normal;
            text-decoration: none;
            color: #007bff;
            float: right;
            margin-top: 4px;
        }
        .card h3 a:hover {
            text-decoration: underline;
        }

        .info-pair {
            display: flex;
justify-content: space-between;
            font-size: 1.1em;
            padding: 1em 0.5em;
            border-bottom: 1px solid #f0f0f0;
}
        .info-pair:last-child { border-bottom: none;
}
        .info-pair strong { color: #333;
}
        .info-pair span { color: #555;
}
        
        .info-pair .status-Active { 
            color: #28a745; 
            font-weight: bold; 
        }
        .info-pair .status-Expired { 
            color: #dc3545; 
            font-weight: bold; 
        }

        .payment-method {
            display: flex;
justify-content: space-between;
            align-items: center;
            padding: 1em;
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            margin-bottom: 1em;
}
        .payment-info { font-weight: bold;
}
        .payment-info .default { color: #28a745; font-size: 0.9em; margin-left: 1em;
}
        .payment-actions form { display: inline-block; margin-left: 10px;
}
        .action-btn {
            padding: 8px 12px;
border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            color: white;
            text-decoration: none;
}
        .btn-delete { background-color: #dc3545;
}
        .btn-default { background-color: #007bff;
}
        .btn-default:disabled { background-color: #6c757d; cursor: not-allowed;
}
        .btn-receipt {
            display: inline-block;
background-color: #17a2b8;
            color: white;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
            margin-top: 1em;
}
        /* New button for Renew */
        .btn-renew {
            display: inline-block;
background-color: #28a745;
            color: white;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
            margin-top: 1em;
            margin-left: 10px;
/* Space it from the other button */
            border: none;
font-family: sans-serif;
            font-size: 0.9em;
        }
        .btn-renew:hover {
            background-color: #218838;
}
        .card label {
            display: block;
margin-bottom: 5px;
            font-weight: bold;
        }
        .card input[type="text"],
        .card input[type="tel"],
        .card input[type="password"],
        .card select {
            width: 100%;
padding: 10px;
            box-sizing: border-box;
            border-radius: 4px;
            border: 1px solid #ccc;
}
        .form-grid {
            display: grid;
grid-template-columns: 1fr 1fr;
            gap: 1em;
        }
        .payment-choice {
            display: flex;
gap: 1em;
            margin-bottom: 1em;
        }
        .payment-choice label {
            display: flex;
align-items: center;
            gap: 0.5em;
            font-weight: normal; 
        }
        .payment-choice input[type="radio"] {
            width: auto;
}
        .payment-details-group {
            background-color: #f8f9fa;
border: 1px solid #eee;
            padding: 1.5em;
            border-radius: 4px;
            margin-top: 1em;
}
        .checkbox-label {
            display: flex;
align-items: center;
            gap: 0.5em;
            font-weight: normal;
            margin-top: 1.5em;
        }
        .checkbox-label input[type="checkbox"] {
            width: auto;
}
        .input-error {
            border-color: #dc3545;
}
        .error-message {
            color: #dc3545;
font-size: 0.9em;
            font-weight: bold;
            display: block;
            margin-top: 5px;
        }
        .form-submit-btn {
            padding: 12px 20px;
background-color: #28a745; /* Green for "add" */
            color: white;
border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1em;
            font-weight: bold;
            margin-top: 1.5em;
}
        .form-submit-btn:hover {
            background-color: #218838;
}
        
        /* Message styles for success and error */
        .message-success {
            padding: 1em;
border-radius: 4px;
            margin-bottom: 1.5em;
            font-weight: bold;
            border: 1px solid #c3e6cb;
            color: #155724;
            background-color: #d4edda;
}
        .message-error {
            padding: 1em;
border-radius: 4px;
            margin-bottom: 1.5em;
            font-weight: bold;
            border: 1px solid #f5c6cb;
            color: #721c24;
            background-color: #f8d7da;
}
        .renewal-note {
            font-style: italic;
color: #555;
            margin-top: 1em;
            padding: 10px;
            background-color: #f8f9fa;
            border-radius: 4px;
}
    </style>
</head>
<body>
    <%- include('partials/member-header') %>
    <div class="container">
        <h1>Manage Your Account</h1>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="message-success"><%= success %></div>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="message-error"><%= error %></div>
 
       <% } %>

        <div class="card">
            <h3>Membership Details <a href="/member/edit">Edit Profile</a></h3>
            <div class="info-pair">
                <strong>Member Name:</strong>
                <span><%= member.first_name %> <%= member.last_name %></span>
            </div>
      
      <div class="info-pair">
                <strong>Membership Type:</strong>
                <span><%= member.type_name %></span>
            </div>
            <div class="info-pair">
                <strong>Status:</strong>
                <span 
class="status-<%= member.member_status %>"><%= member.member_status %></span>
            </div>
            <div class="info-pair">
                <strong>Valid Through:</strong>
                <span><%= new Date(member.end_date).toLocaleDateString() %></span>
            </div>
            
           
 <a href="/member/purchases" class="btn-receipt">View Membership Purchases</a>
            
            <% if (canRenew) { %>
                <% if (paymentMethods.length > 0) { %>
                    <a href="/member/renew" class="btn-renew" style="text-decoration: none; font-size: 1em;">
                        Renew Membership
                    </a>
                <% } else { %>
                    <p class="renewal-note" style="color: #721c24; border: 1px solid #f5c6cb; background-color: #f8d7da;">
                        <strong>You must add a payment method before you can renew.</strong>
                        <br>
                        <i>Please add a card or bank account below.</i>
                    </p>
                <% } %>
            <% } else { %>
                <p class="renewal-note">
                    <i>You can renew your membership when it is within 60 days of its expiration date.</i>
                </p>
            <% } %>
         
   </div>

        <div class="card">
            <h3>Saved Payment Methods</h3>