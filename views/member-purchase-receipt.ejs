<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Membership Purchase Receipt</title>
    <style>
        body {
            font-family: 'Courier New', Courier, monospace;
            margin: 0;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 2em;
        }
        h1 {
            font-weight: bold;
            margin: 0;
        }
        .receipt-container {
            width: 400px;
            background: #ffffff;
            border: 1px solid #ccc;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            padding: 2em;
            box-sizing: border-box;
        }
        .receipt-header {
            text-align: center;
            border-bottom: 2px dashed #333;
            padding-bottom: 1em;
            margin-bottom: 1em;
        }
        .receipt-header p {
            margin: 4px 0;
            font-size: 0.9em;
        }
        .receipt-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.75em;
            font-size: 0.95em;
        }
        .receipt-item .label {
            font-weight: bold;
        }
        .receipt-item .value {
            text-align: right;
        }
        .receipt-total {
            border-top: 2px solid #333;
            margin-top: 1.5em;
            padding-top: 1em;
        }
        .receipt-total .receipt-item .label,
        .receipt-total .receipt-item .value {
            font-size: 1.2em;
            font-weight: bold;
            color: #000;
        }
        .receipt-footer {
            text-align: center;
            font-size: 0.85em;
            margin-top: 2em;
            border-top: 2px dashed #333;
            padding-top: 1em;
        }
        .close-button {
            display: block;
            width: 400px;
            box-sizing: border-box;
            padding: 12px 20px;
            background-color: #17a2b8;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1em;
            font-weight: bold;
            text-decoration: none;
            text-align: center;
            margin-top: 1.5em;
            font-family: sans-serif;
        }
        .close-button:hover {
            background-color: #138496;
        }
    </style>
</head>
<body>

    <div class="receipt-container">
        <div class="receipt-header">
            <h1>PRIMUS THEME PARK</h1>
            <p>123 Adventure Way, Orlando, FL</p>
            <p>Membership Purchase Receipt</p>
        </div>

        <div class="receipt-body">
            <div class="receipt-item">
                <span class="label">Member ID:</span>
                <span class="value"><%= purchase.membership_id %></span>
            </div>
            <div class="receipt-item">
                <span class="label">Member Name:</span>
                <span class="value"><%= purchase.first_name %> <%= purchase.last_name %></span>
            </div>
            <div class="receipt-item">
                <span class="label">Purchase Date:</span>
                <span class="value"><%= new Date(purchase.start_date).toLocaleDateString() %></span>
            </div>
            <div class="receipt-item">
                <span class="label">Item:</span>
                <span class="value"><%= purchase.type_name %></span>
            </div>
            
            <div class="receipt-total">
                <div class="receipt-item">
                    <span class="label">TOTAL:</span>
                    <span class="value">$<%= parseFloat(purchase.base_price).toFixed(2) %></span>
                </div>
            </div>
        </div>

        <div class="receipt-footer">
            <p>Thank you for being a member!</p>
        </div>
    </div>

    <a href="/member/manage" class="close-button">&laquo Back to Manage Account</a>

</body>
</html>