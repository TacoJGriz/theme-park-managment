<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Promotions | Primus Park</title>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --primary: #6f42c1;
            --accent: #17a2b8;
            --dark: #343a40;
            --light: #f8f9fa;
            --bg-page: #f0f2f5; /* Darker than light for contrast */
            --white: #ffffff;
            --radius: 12px;
            --shadow-md: 0 4px 15px rgba(0,0,0,0.05);
            --transition: all 0.3s ease;
            --border-color: #e0e0e0;
        }

        body { 
            font-family: 'Poppins', sans-serif; 
            margin: 0; 
            background-color: var(--bg-page); /* UPDATED: Better contrast */
            color: var(--dark); 
        }
        .container { max-width: 1200px; margin: 3em auto; padding: 0 2em; }
        
        h1 { 
            font-family: 'Lobster', cursive; color: var(--dark); font-size: 3em;
            border-bottom: 2px solid #eee; padding-bottom: 0.5em; margin-bottom: 1.5em;
        }
        
        .card-container {
            display: grid; 
            /* UPDATED: Wider columns (380px) to fit date string */
            grid-template-columns: repeat(auto-fill, minmax(380px, 1fr)); 
            gap: 25px;
        }
        
        /* Magic Promo Card */
        .card {
            background: var(--white);
            border-radius: var(--radius);
            box-shadow: var(--shadow-md);
            border-top: 5px solid var(--primary);
            /* UPDATED: Added border for better definition against white/light backgrounds */
            border-left: 1px solid var(--border-color);
            border-right: 1px solid var(--border-color);
            border-bottom: 1px solid var(--border-color);
            transition: transform 0.2s;
            display: flex; flex-direction: column;
            overflow: hidden;
        }
        .card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(0,0,0,0.1); }
        
        .card-body { padding: 2em; flex-grow: 1; display: flex; flex-direction: column; }
        
        .tag {
            display: inline-block; background: #e9ecef; padding: 4px 12px; border-radius: 20px;
            font-size: 0.8em; font-weight: 700; margin-bottom: 15px; color: #495057; text-transform: uppercase;
            align-self: flex-start;
        }
        
        .card h3 { margin: 0 0 0.5em 0; font-size: 1.4em; color: var(--dark); font-weight: 700; }
        
        .offer {
            font-size: 2.5em; font-weight: 800; color: var(--primary); margin: 0.2em 0 0.5em 0; line-height: 1;
        }
        .offer-sub { font-size: 0.4em; font-weight: 600; color: #666; text-transform: uppercase; display: block; margin-top: 5px; }
        
        .summary { color: #555; line-height: 1.6; margin-bottom: 1.5em; flex-grow: 1; }
        
        .card-footer {
            background-color: #f8f9fa; padding: 1em 2em; font-size: 0.9em; color: #666; border-top: 1px solid #eee;
            display: flex; align-items: center; gap: 8px;
            /* UPDATED: Prevent wrapping of date string */
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
    </style>
</head>
<body>
    <%- include('partials/member-header') %>
    <div class="container">
        <h1>Current & Upcoming Promotions</h1>
        
        <div class="card-container">
            <% if (promotions.length > 0) { %>
                <% promotions.forEach(promo => { %>
                    <div class="card">
                        <div class="card-body">
                            <span class="tag"><%= promo.event_type %> Event</span>
                            <h3><%= promo.event_name %></h3>
                            <div class="offer">
                                <%= parseFloat(promo.discount_percent).toFixed(0) %>% 
                                <span class="offer-sub">Discount on Tickets</span>
                            </div>
                            <p class="summary"><%= promo.summary %></p>
                        </div>
                        <div class="card-footer">
                            <i class="fa-regular fa-calendar"></i> 
                            <strong>Valid:</strong> <%= new Date(promo.start_date).toLocaleDateString() %> - <%= new Date(promo.end_date).toLocaleDateString() %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div style="grid-column: 1/-1; text-align: center; padding: 4em; color: #666;">
                    <h3>No special promotions are running today.</h3>
                    <p>Check back soon for magical offers!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>