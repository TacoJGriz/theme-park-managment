<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Promotions</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 900px; margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em; }
        
        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }
        .card {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            border-left: 5px solid #28a745;
        }
        .card h3 { margin-top: 0; font-size: 1.4em; color: #333; }
        .card .offer {
            font-size: 2em;
            font-weight: bold;
            color: #218838;
            margin: 0.5em 0;
        }
        .card .offer-sub { font-size: 1em; font-weight: normal; color: #666; }
    </style>
</head>
<body>
    <%- include('partials/member-header') %>
    <div class="container">
        <h1>Current & Upcoming Promotions</h1>
        <div class="card-container">
            <% if (promotions.length > 0) { %>
                <% promotions.forEach(promo => { %>
                    <div class="card promo">
                        <h3><%= promo.event_name %></h3>
                        <div class="offer"><%= parseFloat(promo.discount_percent).toFixed(0) %>% <span class="offer-sub">OFF</span></div>
                        <p><%= promo.summary %></p>
                        <p><strong>Type:</strong> <%= promo.event_type %></p>
                        <p><strong>Valid:</strong> <%= new Date(promo.start_date).toLocaleDateString() %> - <%= new Date(promo.end_date).toLocaleDateString() %></p>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No special promotions are running today. Check back soon!</p>
            <% } %>
        </div>
    </div>
</body>
</html>