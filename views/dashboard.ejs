<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>    
    <style>        
        .dashboard-container { padding: 2em;
            max-width: 1400px; margin: 0 auto; }
        
        h1 { color: #444;
            margin-bottom: 0.2em; }
        
        .welcome-message { 
            font-size: 1.1em;
            margin-bottom: 2em; padding-bottom: 1em;
            border-bottom: 1px solid #ddd; color: #666;
        }

        .assignment-info {
            background-color: #e7f5ff;
            color: #004085;
            border-left: 5px solid #007bff;
            padding: 1.5em;
            border-radius: 4px;
            margin-bottom: 2em;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }
        .assignment-info strong { display: block; font-size: 1.1em; margin-bottom: 0.5em;
        }

        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .card {
            background: white;
            padding: 0;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            transition: transform 0.2s, box-shadow 0.2s;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            border-top: 4px solid #ccc;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .card-header {
            padding: 1.2em 1.5em;
            background-color: #fff;
            border-bottom: 1px solid #f0f0f0;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .card-header h3 { margin: 0; font-size: 1.2em; color: #333;
        }
        .card-icon { font-size: 1.4em; width: 30px; text-align: center;
        }

        .card-body {
            padding: 1.2em 1.5em;
            flex-grow: 1;
        }

        .card a {
            display: block;
            text-decoration: none;
            color: #555;
            padding: 8px 10px;
            border-radius: 6px;
            margin-bottom: 8px;
            transition: background-color 0.2s, color 0.2s, padding-left 0.2s;
            font-weight: 500;
        }
        .card a:hover {
            background-color: #f8f9fa;
            color: #007bff;
            padding-left: 15px;
        }
        .card a i {
            margin-right: 8px;
            color: #adb5bd;
            width: 20px;
            text-align: center;
        }
        .card a:hover i { color: #007bff;
        }

        .card.rides { border-top-color: #dc3545;
        }
        .card.rides .card-icon { color: #dc3545;
        }

        .card.guests { border-top-color: #28a745;
        }
        .card.guests .card-icon { color: #28a745;
        }

        .card.admin { border-top-color: #007bff;
        }
        .card.admin .card-icon { color: #007bff;
        }

        .card.events { border-top-color: #6f42c1;
        }
        .card.events .card-icon { color: #6f42c1;
        }

        .card.retail { border-top-color: #fd7e14;
        }
        .card.retail .card-icon { color: #fd7e14;
        }

        .card.hr { border-top-color: #17a2b8;
        }
        .card.hr .card-icon { color: #17a2b8;
        }
        
        .card.reports { border-top-color: #6610f2;
        }
        .card.reports .card-icon { color: #6610f2;
        }

        .important-link {
            color: #d39e00 !important;
            font-weight: bold !important;
        }
        
        .notification-note {
            margin-top: 15px; 
            padding: 12px; 
            background-color: #fff3cd; 
            border-left: 4px solid #ffc107; 
            border-radius: 4px; 
            color: #856404;
            font-size: 0.9em;
            line-height: 1.4;
        }
    </style>
</head>
<body>

    <%- include('partials/header') %>

    <div class="dashboard-container">
        <h1>Park Management Dashboard</h1>

        <% if (user) { %>
            <p class="welcome-message">
                Hello, <strong><%= user.firstName %> <%= user.lastName %></strong>!
                You are logged in as <strong><%= user.role %></strong>.
            </p>

            <% if ((user.role === 'Staff' || user.role === 'Location Manager') && user.locationName) { %>
                <div class="assignment-info">
                    <strong><i class="fa-solid fa-location-dot"></i> Assignment: <%= user.locationName %></strong>
                    
                    <div style="margin-top: 10px; font-size: 0.95em;">
                        <% if (typeof assignedRides !== 'undefined' && assignedRides.length > 0) { %>
                            <div>
                                <span style="font-weight: bold; color: #555;">Attractions:</span> 
                                <%= assignedRides.map(r => r.ride_name).join(', ') %>
                            </div>
                        <% } %>
            
                        <% if (typeof assignedVendors !== 'undefined' && assignedVendors.length > 0) { %>
                            <div style="margin-top: 4px;">
                                <span style="font-weight: bold; color: #555;">Retail & Food:</span> 
                                <%= assignedVendors.map(v => v.vendor_name).join(', ') %>
                            </div>
                        <% } %>
                        
                        <% if (assignedRides.length === 0 && assignedVendors.length === 0) { %>
                            <div style="margin-top: 4px; font-style: italic; color: #777;">
                                No rides or vendors currently assigned to this location.
                            </div>
                        <% } %>
                    </div>
                </div>
            <% } %>

            <div class="card-container">

            <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Location Manager' || user.role === 'Maintenance' || user.role === 'Staff') { %>
                <div class="card rides">
                    <div class="card-header">
                        <i class="fa-solid fa-wrench card-icon"></i>
                        <h3>Rides & Maintenance</h3>
                    </div>
                    <div class="card-body">
                        <a href="/rides"><i class="fa-solid fa-list"></i> View Rides</a>

                        <% if (user.role === 'Maintenance' && typeof maintenanceCount !== 'undefined' && maintenanceCount > 0) { %>
                            <div class="notification-note">
                                <i class="fa-solid fa-triangle-exclamation"></i> 
                                <strong>Action Required:</strong> You have <strong><%= maintenanceCount %></strong> active work order<%= maintenanceCount === 1 ? '' : 's' %>.
                                <div style="margin-top:5px;"><small>Check the rides list for broken items assigned to you.</small></div>
                            </div>
                        <% } %>

                        <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Location Manager') { %>
                            <% 
                                const hasApprovals = (typeof approvalCount !== 'undefined' && approvalCount > 0);
                                const linkColorStyle = hasApprovals ? 'color: #dc3545; font-weight: 700;' : ''; 
                                const iconColorStyle = hasApprovals ? 'color: #dc3545;' : '';
                            %>
                            
                            <a href="/approvals" style="display: flex; align-items: center; <%= linkColorStyle %>">
                                <i class="fa-solid fa-clipboard-check" style="<%= iconColorStyle %>"></i> 
                                <span style="flex-grow: 1;">Pending Approvals</span>
                                
                                <% if (hasApprovals) { %>
                                    <span style="
                                            background-color: #dc3545; 
                                            color: white; 
                                            font-size: 0.8em; 
                                            font-weight: bold; 
                                            padding: 2px 8px; 
                                            border-radius: 12px;
                                            margin-left: 8px;
                                    "><%= approvalCount %></span>
                                    
                                    <% if (typeof newApprovalCount !== 'undefined' && newApprovalCount > 0) { %>
                                        <span style="
                                            background-color: #ffc107;
                                            color: #333; 
                                            font-size: 0.65em; 
                                            font-weight: 800; 
                                            padding: 2px 6px; 
                                            border-radius: 4px; 
                                            margin-left: 5px;
                                            text-transform: uppercase;
                                        ">NEW</span>
                                    <% } %>
                                <% } %>
                            </a>
                        <% } %>
                    </div>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Staff') { %>
                 <div class="card guests">
                    <div class="card-header">
                        <i class="fa-solid fa-users card-icon"></i>
                        <h3>Guest Services</h3>
                    </div>
                    <div class="card-body">
                        <a href="/members"><i class="fa-solid fa-address-card"></i> Manage Members</a>
                        <a href="/visits/new"><i class="fa-solid fa-ticket"></i> Log Park Entry</a>
                    </div>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Park Manager') { %>
                <div class="card admin">
                    <div class="card-header">
                        <i class="fa-solid fa-building-shield card-icon"></i>
                        <h3>Park Administration</h3>
                    </div>
                    <div class="card-body">
                        <a href="/locations"><i class="fa-solid fa-map-location"></i> Manage Locations</a>
                        <a href="/map"><i class="fa-solid fa-map"></i> Interactive Map</a>
                        <a href="/vendors"><i class="fa-solid fa-shop"></i> Manage Vendors</a>
                        <a href="/memberships/types"><i class="fa-solid fa-id-badge"></i> Membership Types</a>
                        <a href="/ticket-types"><i class="fa-solid fa-tags"></i> Ticket Types</a>
                    </div>
                </div>
                
                <div class="card events">
                    <div class="card-header">
                        <i class="fa-solid fa-calendar-days card-icon"></i>
                        <h3>Park Events</h3>
                    </div>
                    <div class="card-body">
                        <a href="/promotions"><i class="fa-solid fa-bullhorn"></i> Manage Promotions</a>
                        <a href="/weather"><i class="fa-solid fa-cloud-bolt"></i> Weather Events</a>
                    </div>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Location Manager' || user.role === 'Staff') { %>
                 <div class="card retail">
                    <div class="card-header">
                        <i class="fa-solid fa-cart-shopping card-icon"></i>
                        <h3>Retail & Inventory</h3>
                    </div>
                    <div class="card-body">
                        <% if (user.role !== 'Staff') { %>
                            <a href="/items"><i class="fa-solid fa-barcode"></i> Master Item List</a>
                        <% } %>
                        <a href="/vendors"><i class="fa-solid fa-shop"></i> View Vendors</a>
                        <a href="/inventory"><i class="fa-solid fa-boxes-stacked"></i> View Inventory</a>
                        <a href="/inventory/requests"><i class="fa-solid fa-truck-ramp-box"></i> Restock Requests</a>
                    </div>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Park Manager') { %>
                <div class="card reports">
                    <div class="card-header">
                        <i class="fa-solid fa-chart-pie card-icon"></i>
                        <h3>Reports & Analytics</h3>
                    </div>
                    <div class="card-body">
                        <a href="/reports/attendance"><i class="fa-solid fa-users-line"></i> Attendance Report</a>
                        <a href="/reports/ride-popularity"><i class="fa-solid fa-star"></i> Ride Popularity</a>
                        <a href="/reports/closure-impact"><i class="fa-solid fa-cloud-showers-heavy"></i> Closure Impact</a>
                        <a href="/reports/maintenance"><i class="fa-solid fa-screwdriver-wrench"></i> Maintenance Report</a>
                    </div>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Location Manager') { %>
                <div class="card hr">
                    <div class="card-header">
                        <i class="fa-solid fa-user-tie card-icon"></i>
                        <h3>Human Resources</h3>
                    </div>
                    <div class="card-body">
                        <a href="/users"><i class="fa-solid fa-users-viewfinder"></i> Manage Employees</a>
                    </div>
                </div>
            <% } %>

        </div>
        <% } %>
    </div>
</body>
</html>
