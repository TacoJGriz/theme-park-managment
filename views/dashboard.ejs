<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .dashboard-container { padding: 2em; }
        h1 { color: #444; }
        .welcome-message { font-size: 1.2em; margin-bottom: 1.5em; padding-bottom: 0.5em; border-bottom: 1px solid #ddd;}
        .card-container { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; margin-top: 1em; }
        .card { background: white; padding: 20px; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .card h3 { margin-top: 0; border-bottom: 1px solid #eee; padding-bottom: 10px; font-size: 1.1em; color: #333; }
        .card a { display: block; text-decoration: none; color: #007bff; padding: 8px 5px; border-radius: 4px; margin-bottom: 5px; }
        .card a:hover { background-color: #e9ecef; color: #0056b3; }
        .card a::before { content: '» '; color: #6c757d; }
        .approval-link { background-color: #fff3cd; font-weight: bold; border: 1px solid #ffeeba; }
        .approval-link:hover { background-color: #ffeeba; }
    </style>
</head>
<body>

    <%- include('partials/header') %>

    <div class="dashboard-container">
        <h1>Park Management Dashboard</h1>

        <% if (user) { %>
            <p class="welcome-message">
                Hello, <strong><%= user.firstName %> <%= user.lastName %></strong>! You are logged in as an <strong><%= user.role %></strong>.
            </p>
        <% } %>

        <div class="card-container">
            
            <% if ((user.role === 'Admin' || user.role === 'Manager') && approvalCount > 0) { %>
                <div class="card">
                    <h3>Approvals</h3>
                    <a href="/approvals" class="approval-link">View <%= approvalCount %> Pending Requests</a>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Manager' || user.role === 'Maintenance' || user.role === 'Staff') { %>
                <div class="card">
                    <h3>Rides & Maintenance</h3>
                    <a href="/rides">View All Rides</a>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Manager' || user.role === 'Staff') { %>
                <div class="card">
                    <h3>Guest Services</h3>
                    <a href="/members">Manage Members</a>
                    <a href="/visits/new">Log Park Visit</a>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'Manager') { %>
                <div class="card">
                    <h3>Park & Retail Operations</h3>
                    <a href="/locations">Manage Locations</a>
                    <a href="/vendors">Manage Vendors</a>
                    <a href="/items">Manage Items</a>
                    <a href="/inventory">Manage Inventory</a>
                </div>
                <div class="card">
                    <h3>Park Events</h3>
                    <a href="/promotions">Manage Promotions</a>
                    <a href="/weather">Manage Weather Events</a>
                </div>
                <div class="card">
                    <h3>Park Reports</h3>
                    <a href="/reports/attendance">Attendance Report</a>
                </div>
            <% } %>

            <% if (user.role === 'Admin' || user.role === 'HR') { %>
                <div class="card">
                    <h3>Human Resources</h3>
                    <a href="/users">Manage Employees</a>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>