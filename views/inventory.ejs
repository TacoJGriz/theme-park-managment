<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Inventory</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 1100px; margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em; }
        
        table { width: 100%; border-collapse: collapse; margin-top: 1.5em; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #343a40; color: white; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        tr:hover { background-color: #f1f1f1; }
        
        .button-bar { display: flex; justify-content: flex-end; margin-bottom: 1.5em; }
        .action-button { 
            padding: 12px 20px; background-color: #007bff; color: white; 
            text-decoration: none; border-radius: 4px; font-weight: bold;
        }
        .action-button:hover { background-color: #0056b3; }

        /* New Button Styles for Table Actions */
        .btn-action {
            display: inline-block; padding: 6px 12px; border-radius: 4px;
            text-decoration: none; font-weight: bold; font-size: 0.9em;
            margin-right: 5px; border: none; cursor: pointer; color: white;
        }
        .btn-restock { background-color: #007bff; }
        .btn-restock:hover { background-color: #0056b3; }
        
        .btn-destock { background-color: #dc3545; }
        .btn-destock:hover { background-color: #c82333; }
        
        .btn-deshelf { background-color: #6c757d; }
        .btn-deshelf:hover { background-color: #5a6268; }

        .btn-pending { background-color: #ffc107; color: #333; cursor: not-allowed; pointer-events: none; }

        /* Drill-down link style */
        .manage-link {
            display: inline-block; padding: 6px 12px;
            background-color: #17a2b8;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
            font-size: 0.9em;
        }
        .manage-link:hover { background-color: #138496; }

        .status-Pending { color: #d39e00; font-weight: bold; }
        .status-Low { color: #dc3545; font-weight: bold; }
        .status-OK { color: #28a745; font-weight: bold; }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h1>Park Inventory</h1>
        
        <div class="button-bar">
            <a href="/inventory/requests" class="action-button">View All Restock Requests</a>
        </div>

        <table>
            <thead>
                <tr>
                    <th>Vendor</th>
                    <th>Item</th>
                    <th>Current Count</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (inventory.length === 0) { %>
                    <tr><td colspan="4" style="text-align: center; padding: 2em;">No inventory records found.</td></tr>
                <% } %>
                <% inventory.forEach(item => { %>
                    <tr>
                        <td><%= item.vendor_name %></td>
                        <td><%= item.item_name %></td>
                        <td><%= item.current_count %></td>
                        
                        <td>
                            <%# --- 1. RESTOCK (Visible to All) --- %>
                            <% if (item.pending_count) { %>
                                <span class="btn-action btn-pending">Restock Pending</span>
                            <% } else { %>
                                <a href="/inventory/request/<%= item.public_vendor_id %>/<%= item.public_item_id %>?returnTo=inventory" class="btn-action btn-restock">Restock</a>
                            <% } %>

                            <%# --- 2. DESTOCK & DESHELF (Managers Only) --- %>
                            <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Location Manager') { %>
                                <a href="/inventory/destock/<%= item.public_vendor_id %>/<%= item.public_item_id %>?returnTo=inventory" class="btn-action btn-destock">Destock</a>
        
                                <form action="/inventory/deshelf/<%= item.public_vendor_id %>/<%= item.public_item_id %>?returnTo=inventory" method="POST" style="display: inline-block;" onsubmit="return confirm('Are you sure you want to remove this item from the shelf?');">
                                    <button type="submit" class="btn-action btn-deshelf">Deshelf</button>
                                </form>
                            <% } %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>