<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Inventory</title>
    <style>
        body { font-family: sans-serif;
            margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 1100px;
            margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em;
        }
        
        /* Stats Bar Styles */
        .stats-bar {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1em;
            margin-bottom: 1.5em;
        }
        .stat-card {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 1.5em;
            border-radius: 8px;
            text-align: center;
        }
        .stat-card h3 { margin: 0 0 0.25em 0; font-size: 2em; color: #007bff;
        }
        .stat-card h3.warning { color: #dc3545;
        }
        .stat-card h3.pending { color: #d39e00;
        }
        .stat-card p { margin: 0; font-weight: bold; color: #6c757d;
        }

        /* Filter Bar Styles */
        .filter-bar {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 1.5em;
            border-radius: 8px;
            margin-bottom: 1.5em;
        }
        .filter-controls {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 1em;
            align-items: flex-end;
        }
        .filter-controls label { font-weight: bold; display: block; margin-bottom: 5px;
            font-size: 0.9em; }
        .filter-controls input, .filter-controls select {
            width: 100%;
            padding: 10px; border: 1px solid #ccc; border-radius: 4px; font-size: 1em; box-sizing: border-box;
        }
        .filter-buttons { display: flex; gap: 10px;
        }
        .filter-btn {
            flex-grow: 1;
            padding: 10px; color: white; border: none; border-radius: 4px; cursor: pointer; font-weight: bold; text-align: center; text-decoration: none;
        }
        .btn-filter { background-color: #007bff;
        }
        .btn-filter:hover { background-color: #0056b3;
        }
        .btn-clear { background-color: #6c757d;
        }
        .btn-clear:hover { background-color: #5a6268;
        }

        table { width: 100%;
            border-collapse: collapse; margin-top: 1.5em; }
        th, td { padding: 12px; text-align: left;
            border-bottom: 1px solid #ddd; }
        th { background-color: #343a40; color: white;
        }
        
        /* Sortable Header Links */
        th.sortable a {
            text-decoration: none;
            color: white; display: flex; justify-content: space-between; align-items: center;
        }
        th.sortable a:hover { text-decoration: underline;
        }
        .sort-indicator { font-size: 1.2em; margin-left: 5px;
        }

        tr:nth-child(even) { background-color: #f9f9f9;
        }
        tr:hover { background-color: #f1f1f1;
        }
        
        .button-bar { display: flex;
            justify-content: flex-end; margin-bottom: 1.5em; }
        .action-button { 
            padding: 12px 20px;
            background-color: #007bff; color: white; 
            text-decoration: none; border-radius: 4px; font-weight: bold;
        }
        .action-button:hover { background-color: #0056b3;
        }

        /* --- NEW ACTION BUTTON STYLES --- */
        .btn-action {
            display: block;
            width: 100%;
            box-sizing: border-box;
            padding: 5px 10px;
            margin-bottom: 5px;
            border-radius: 4px;
            text-align: center;
            text-decoration: none;
            font-weight: bold;
            font-size: 0.85em;
            border: 1px solid transparent;
            cursor: pointer;
            background-color: white;
            transition: all 0.2s;
            font-family: inherit;
        }
        .btn-action:last-child { margin-bottom: 0; }

        .btn-restock {
            color: #007bff;
            border-color: #007bff;
        }
        .btn-restock:hover { background-color: #007bff; color: white; }
        
        .btn-destock {
            color: #dc3545;
            border-color: #dc3545;
        }
        .btn-destock:hover { background-color: #dc3545; color: white; }
        
        .btn-deshelf {
            color: #6c757d;
            border-color: #6c757d;
        }
        .btn-deshelf:hover { background-color: #6c757d; color: white; }

        .btn-pending {
            background-color: #fff3cd;
            color: #856404;
            border: 1px solid #ffeeba;
            cursor: not-allowed;
            pointer-events: none;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h1>Park Inventory</h1>

        <%# --- STATS BAR --- %>
        <div class="stats-bar">
            <div class="stat-card">
                <h3><%= counts.total_records || 0 %></h3>
                <p>Total Records</p>
            </div>
            <div class="stat-card">
                <h3 class="warning"><%= counts.low_stock_count || 0 %></h3>
                <p>Low Stock (&lt;10)</p>
            </div>
            <div class="stat-card">
                <h3 class="pending"><%= counts.pending_count || 0 %></h3>
                <p>Pending Requests</p>
            </div>
        </div>
        
        <%# --- FILTER BAR --- %>
        <div class="filter-bar">
            <form action="" method="GET">
                <div class="filter-controls">
                    <div>
                        <label for="search">Search</label>
                        <input type="text" id="search" name="search" placeholder="Item or Vendor Name..." value="<%= search %>">
                    </div>
                    <div>
                        <label for="filter_vendor">Filter by Vendor</label>
                        <select id="filter_vendor" name="filter_vendor">
                            <option value="">All Vendors</option>
                            <% vendors.forEach(v => { %>
                                <option value="<%= v.public_vendor_id %>" <%= filters.vendor === v.public_vendor_id ? 'selected' : '' %>>
                                    <%= v.vendor_name %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                    <div>
                        <label for="filter_status">Status</label>
                        <select id="filter_status" name="filter_status">
                            <option value="">All</option>
                            <option value="ok" <%= filters.status === 'ok' ? 'selected' : '' %>>OK</option>
                            <option value="low_stock" <%= filters.status === 'low_stock' ? 'selected' : '' %>>Low Stock</option>
                            <option value="pending" <%= filters.status === 'pending' ? 'selected' : '' %>>Pending Request</option>
                        </select>
                    </div>
                    <div class="filter-buttons">
                        <button type="submit" class="filter-btn btn-filter">Filter</button>
                        <a href="?" class="filter-btn btn-clear">Clear</a>
                    </div>
                </div>
                
                <%# Maintain sort state %>
                <% if (currentSort && currentDir) { %>
                    <input type="hidden" name="sort" value="<%= currentSort %>">
                    <input type="hidden" name="dir" value="<%= currentDir %>">
                <% } %>
            </form>
        </div>

        <div class="button-bar">
            <% if (role === 'Admin' || role === 'Park Manager' || role === 'Location Manager') { %>
                <a href="/inventory/add" class="action-button" style="background-color: #28a745; margin-right: 10px;">Add Item to Shelf</a>
            <% } %>
            <a href="/inventory/requests" class="action-button">View All Restock Requests</a>
        </div>

        <% 
            const getSortState = (column) => {
                let nextDir = 'asc';
                let indicator = '';
                if (currentSort === column) {
                    if (currentDir === 'asc') {
                        nextDir = 'desc';
                        indicator = '▲'; 
                    } else if (currentDir === 'desc') {
                        nextDir = 'none';
                        indicator = '▼';
                    }
                }
                let href = `?search=${encodeURIComponent(search)}`;
                if (filters.vendor) href += `&filter_vendor=${encodeURIComponent(filters.vendor)}`;
                if (filters.status) href += `&filter_status=${encodeURIComponent(filters.status)}`;
                if (nextDir !== 'none') {
                    href += `&sort=${column}&dir=${nextDir}`;
                }
                return { href: href, indicator: indicator };
            };
        %>

        <table>
            <thead>
                <tr>
                    <th class="sortable">
                        <a href="<%= getSortState('vendor').href %>">
                            <span>Vendor</span> <span class="sort-indicator"><%= getSortState('vendor').indicator %></span>
                        </a>
                    </th>
                    <th class="sortable">
                        <a href="<%= getSortState('item').href %>">
                            <span>Item</span> <span class="sort-indicator"><%= getSortState('item').indicator %></span>
                        </a>
                    </th>
                    <th class="sortable">
                        <a href="<%= getSortState('count').href %>">
                            <span>Current Count</span> <span class="sort-indicator"><%= getSortState('count').indicator %></span>
                        </a>
                    </th>
                    <th style="min-width: 140px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (inventory.length === 0) { %>
                    <tr><td colspan="4" style="text-align: center; padding: 2em;">No inventory records found.</td></tr>
                <% } %>
         
               <% inventory.forEach(item => { %>
                    <tr>
                        <td><%= item.vendor_name %></td>
                        <td><%= item.item_name %></td>
                        <td><%= item.current_count %></td>
                        
                        <td>
                            <%# --- 1. RESTOCK (Visible to All) --- %>
                            <% if (item.pending_count) { %>
                                <span class="btn-action btn-pending">Restock Pending</span>
                            <% } else { %>
                                <a href="/inventory/request/<%= item.public_vendor_id %>/<%= item.public_item_id %>?returnTo=inventory" class="btn-action btn-restock">Restock</a>
                            <% } %>

                            <%# --- 2. DESTOCK & DESHELF (Managers Only) --- %>
                            <% if (user.role === 'Admin' || user.role === 'Park Manager' || user.role === 'Location Manager') { %>
                                <a href="/inventory/destock/<%= item.public_vendor_id %>/<%= item.public_item_id %>?returnTo=inventory" class="btn-action btn-destock">Destock</a>
        
                                <form action="/inventory/deshelf/<%= item.public_vendor_id %>/<%= item.public_item_id %>?returnTo=inventory" method="POST" style="margin:0;" onsubmit="return confirm('Are you sure you want to remove this item from the shelf?');">
                                    <button type="submit" class="btn-action btn-deshelf">Deshelf</button>
                                </form>
                            <% } %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>