<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Visit Receipt</title>
    <style>
        body {
            font-family: 'Courier New', Courier, monospace;
            margin: 0;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 2em;
        }
        h1 {
            font-weight: bold;
            margin: 0;
        }
        .receipt-container {
            width: 400px;
            background: #ffffff;
            border: 1px solid #ccc;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            padding: 2em;
            box-sizing: border-box;
        }
        .receipt-header {
            text-align: center;
            border-bottom: 2px dashed #333;
            padding-bottom: 1em;
            margin-bottom: 1em;
        }
        .receipt-header p {
            margin: 4px 0;
            font-size: 0.9em;
        }
        .receipt-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.75em;
            font-size: 0.95em;
        }
        .receipt-item .label {
            font-weight: bold;
        }
        .receipt-item .value {
            text-align: right;
        }
        .receipt-total {
            border-top: 2px solid #333;
            margin-top: 1.5em;
            padding-top: 1em;
        }
        .receipt-total .receipt-item .label,
        .receipt-total .receipt-item .value {
            font-size: 1.2em;
            font-weight: bold;
            color: #000;
        }
        .receipt-footer {
            text-align: center;
            font-size: 0.85em;
            margin-top: 2em;
            border-top: 2px dashed #333;
            padding-top: 1em;
        }
        .member-details {
            border-top: 1px solid #eee;
            margin-top: 1.5em;
            padding-top: 1em;
        }
        .member-details h3 {
            margin-top: 0;
            text-align: center;
        }
        .close-button {
            display: block;
            width: 400px;
            box-sizing: border-box;
            padding: 12px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1em;
            font-weight: bold;
            text-decoration: none;
            text-align: center;
            margin-top: 1.5em;
            font-family: sans-serif; /* Use a cleaner font for the button */
        }
        .close-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <div class="receipt-container">
        <div class="receipt-header">
            <h1>PRIMUS THEME PARK</h1>
            <p>123 Adventure Way, Orlando, FL</p>
            <p>Visit Receipt</p>
        </div>

        <div class="receipt-body">
            <div class="receipt-item">
                <span class="label">Visit ID:</span>
                <span class="value"><%= receipt.visit_id %></span>
            </div>
            <div class="receipt-item">
                <span class="label">Date & Time:</span>
                <span class="value"><%= receipt.visit_date %></span>
            </div>
            <div class="receipt-item">
                <span class="label">Ticket Type:</span>
                <span class="value"><%= receipt.ticket_name %></span>
            </div>
            <div class="receipt-item">
                <span class="label">Cashier:</span>
                <span class="value"><%= receipt.staff_name %></span>
            </div>

            <% if (receipt.is_member) { %>
                <div class="member-details">
                    <h3>Member Details</h3>
                    <div class="receipt-item">
                        <span class="label">Member Name:</span>
                        <span class="value"><%= receipt.member_name %></span>
                    </div>
                    <div class="receipt-item">
                        <span class="label">Member ID:</span>
                        <span class="value"><%= receipt.member_id %></span>
                    </div>
                    <div class="receipt-item">
                        <span class="label">Member Type:</span>
                        <span class="value"><%= receipt.member_type %></span>
                    </div>
                    <div class="receipt-item">
                        <span class="label">Phone:</span>
                        <span class="value"><%= receipt.member_phone %></span>
                    </div>
                </div>

                <div class="receipt-total">
                    <div class="receipt-item">
                        <span class="label">TOTAL:</span>
                        <span class="value">$0.00</span>
                    </div>
                </div>
                
            <% } else { %>
                <div class="member-details">
                    <h3>Payment Details</h3>
                    <div class="receipt-item">
                        <span class="label">Base Price:</span>
                        <span class="value">$<%= receipt.base_price.toFixed(2) %></span>
                    </div>
                    <div class="receipt-item">
                        <span class="label">Discount (<%= receipt.promo_applied %>):</span>
                        <span class="value">-$<%= receipt.discount_amount.toFixed(2) %></span>
                    </div>
                </div>

                <div class="receipt-total">
                    <div class="receipt-item">
                        <span class="label">TOTAL:</span>
                        <span class="value">$<%= receipt.total_cost.toFixed(2) %></span>
                    </div>
                </div>
            <% } %>
        </div>

        <div class="receipt-footer">
            <p>Thank you for visiting Primus Theme Park!</p>
            <p>Have a magical day!</p>
        </div>
    </div>

    <% if (user) { %>
        <a href="/visits/new" class="close-button">Log New Visit</a>
    <% } else if (member) { %>
        <a href="/member/history" class="close-button" style="background-color: #17a2b8;">&laquo Back to My History</a>
    <% } %>

</body>
</html>