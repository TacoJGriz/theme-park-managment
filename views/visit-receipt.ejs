<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Visit Confirmation | Primus Park</title>
    <script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Poppins:wght@400;600;800&display=swap" rel="stylesheet">
    
    <style>
        /* --- STANDARDIZED VARIABLES --- */
        :root {
            --ticket-gold: #ffc107; --ticket-dark: #343a40; --ticket-blue: #17a2b8; --bg-color: #222;
            --primary: #6f42c1; --success: #28a745; --light: #f8f9fa; --white: #ffffff; --gray: #6c757d;
            --shadow-lg: 0 15px 35px rgba(0, 0, 0, 0.15); --radius: 16px;
            --accent: #17a2b8; /* Member Theme */
        }

        body { 
            font-family: 'Poppins', sans-serif;
            margin: 0; 
            background-color: var(--light); 
            color: var(--dark);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 2em;
            min-height: 100vh;
        }

        /* Override body bg for Ticket Carousel mode (Dark Mode) */
        body.ticket-mode { background-color: var(--bg-color); color: white; }

        .container { 
            max-width: 500px; 
            width: 95%;
            margin-bottom: 3em;
        }

        .page-header { text-align: center; margin-bottom: 30px; animation: fadeIn 1s; }
        .page-header h1 { font-family: 'Lobster', cursive; font-size: 3em; color: var(--ticket-gold); margin: 0; text-shadow: 0 2px 10px rgba(0,0,0,0.5); }
        
        /* CAROUSEL STYLES */
        .carousel-container { position: relative; width: 100%; max-width: 360px; }
        .ticket-visual { background: white; color: #333; border-radius: 20px; overflow: hidden; box-shadow: 0 15px 40px rgba(0,0,0,0.5); display: none; animation: popIn 0.4s; position: relative; }
        .ticket-visual.active { display: block; }
        .ticket-top { background: var(--ticket-blue); padding: 20px; color: white; text-align: center; border-bottom: 3px dashed rgba(255,255,255,0.5); -webkit-print-color-adjust: exact; print-color-adjust: exact; }
        .park-logo { font-family: 'Lobster', cursive; font-size: 1.5em; opacity: 0.9; }
        .ticket-type-title { font-size: 2em; font-weight: 800; text-transform: uppercase; line-height: 1.1; margin-top: 5px; }
        .ticket-mid { padding: 25px; text-align: center; }
        .info-group { margin-bottom: 15px; }
        .info-label { font-size: 0.75em; color: #888; text-transform: uppercase; font-weight: 700; letter-spacing: 1px; }
        .info-val { font-size: 1.3em; font-weight: 600; color: #222; }
        .ticket-bottom { background: #f8f9fa; padding: 20px; text-align: center; border-top: 1px solid #eee; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
        .barcode-container svg { width: 100%; height: 70px; display: block; }
        .code-text { font-family: 'Courier New', monospace; font-weight: bold; letter-spacing: 0; color: #555; margin-top: 5px; font-size: 0.65em; word-break: break-all; }
        .nav-controls { display: flex; justify-content: space-between; align-items: center; margin-top: 20px; width: 100%; }
        .nav-btn { background: rgba(255,255,255,0.1); border: 2px solid rgba(255,255,255,0.2); color: white; width: 50px; height: 50px; border-radius: 50%; font-size: 1.5em; cursor: pointer; transition: 0.2s; }
        .nav-btn:hover:not(:disabled) { background: var(--ticket-gold); color: #222; border-color: var(--ticket-gold); transform: scale(1.1); }
        .nav-btn:disabled { opacity: 0.3; cursor: default; }
        .counter { font-weight: bold; letter-spacing: 1px; }

        /* LIST STYLES (Standard Receipt) */
        .receipt-card { background: var(--white); border-radius: var(--radius); box-shadow: var(--shadow-lg); overflow: hidden; color: #333; animation: floatUp 0.4s ease-out; width: 100%; }
        .receipt-header { background: linear-gradient(135deg, var(--primary), #5a32a3); color: white; padding: 2.5em 2em; text-align: center; }
        .receipt-header h1 { font-family: 'Lobster', cursive; margin: 0; font-size: 2.8em; text-shadow: 0 2px 5px rgba(0,0,0,0.2); }
        .receipt-header p { margin: 0.2em 0 0 0; font-size: 1em; text-transform: uppercase; letter-spacing: 1.5px; opacity: 0.9; font-weight: 600; }
        
        .receipt-body { padding: 2.5em; }
        .info-row { display: flex; justify-content: space-between; align-items: center; padding: 14px 0; border-bottom: 1px solid #f0f0f0; font-size: 1em; }
        .info-row:last-child { border-bottom: none; }
        .label { color: var(--gray); font-weight: 600; font-size: 0.9em; text-transform: uppercase; }
        .value { font-weight: 600; color: var(--dark); text-align: right; }
        .value.id { font-family: 'Courier New', monospace; letter-spacing: 1px; }
        .value.main { color: var(--primary); font-size: 1.2em; font-weight: 700; }

        /* Guest Pass Highlight */
        .guest-pass-box { background-color: #fff3cd; border: 1px solid #ffeeba; border-radius: 8px; padding: 1em; margin-top: 1.5em; display: flex; justify-content: space-between; align-items: center; }
        .guest-pass-text { color: #856404; font-weight: 600; font-size: 0.9em; }
        .guest-pass-val { color: #856404; font-weight: 700; font-size: 1.1em; }

        .sub-list { background: #f8f9fa; border-radius: 8px; padding: 1.2em; margin-top: 1.5em; border: 1px solid #eee; }
        .sub-item { display: flex; justify-content: space-between; font-size: 0.95em; margin-bottom: 8px; }
        
        .receipt-footer { background: #f8f9fa; padding: 2em; border-top: 2px dashed #ccc; }
        .footer-row { display: flex; justify-content: space-between; align-items: center; }
        .footer-label { font-weight: 700; font-size: 1.2em; }
        .footer-value { font-weight: 700; font-size: 1.8em; color: var(--dark); }

        /* ACTIONS */
        .footer-actions { margin-top: 30px; text-align: center; width: 100%; }
        .btn-print { background: transparent; color: #999; border: 1px solid #ccc; padding: 10px 20px; border-radius: 20px; cursor: pointer; font-size: 0.9em; transition: 0.2s; margin-bottom: 15px; }
        .btn-print:hover { background: var(--dark); color: white; border-color: var(--dark); }
        
        .btn-close { display: block; width: 100%; padding: 16px; text-align: center; background-color: var(--primary); color: white; text-decoration: none; font-weight: 700; border-radius: 30px; transition: all 0.2s; box-shadow: 0 4px 10px rgba(0,0,0,0.1); font-size: 1.1em; }
        .btn-close:hover { filter: brightness(0.9); transform: translateY(-2px); }

        @media print {
            body { background: white !important; color: black !important; display: block !important; padding: 0 !important; height: auto !important; }
            .page-header, .footer-actions, .nav-controls, .btn-close { display: none !important; }
            .carousel-container { max-width: none !important; width: 100% !important; display: block !important; margin: 0 !important; }
            .ticket-visual { display: block !important; opacity: 1 !important; margin: 0 auto 30px auto !important; max-width: 400px !important; page-break-inside: avoid !important; border: 1px solid #ccc !important; }
            .ticket-top, .ticket-bottom { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }
            .ticket-top { background: var(--ticket-blue) !important; color: white !important; }
            .ticket-bottom { background: #f8f9fa !important; }
            .receipt-card { box-shadow: none !important; border: 1px solid #ddd; max-width: 100% !important; }
            .receipt-header { background: #6f42c1 !important; color: white !important; -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }
        }
        @keyframes popIn { 0% { transform: scale(0.9); opacity: 0; } 100% { transform: scale(1); opacity: 1; } }
        @keyframes floatUp { from { transform: translateY(20px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
    </style>
</head>
<body class="<%= (receipt.tickets && receipt.tickets.length > 0) ? 'ticket-mode' : '' %>">

    <% if (receipt.tickets && receipt.tickets.length > 0) { %>
        <div class="page-header">
            <h1>Visit Confirmation</h1>
            <p>Your digital tickets are ready.</p>
        </div>

        <div class="carousel-container">
            <% receipt.tickets.forEach((ticket, index) => { %>
                <div class="ticket-visual <%= index === 0 ? 'active' : '' %>">
                    <div class="ticket-top">
                        <div class="park-logo">Primus Park</div>
                        <div class="ticket-type-title"><%= ticket.name %></div>
                    </div>
                    <div class="ticket-mid">
                        <div class="info-group">
                            <div class="info-label">Valid Date</div>
                            <div class="info-val"><%= receipt.visit_date.split(',')[0] %></div>
                        </div>
                        <% if (ticket.memberName) { %>
                            <div class="info-group">
                                <div class="info-label">Sponsor</div>
                                <div class="info-val" style="font-size:1.1em;"><%= ticket.memberName %></div>
                            </div>
                        <% } %>
                        <div class="info-group">
                            <div class="info-label">Admit</div>
                            <div class="info-val">1 Guest</div>
                        </div>
                    </div>
                    <div class="ticket-bottom">
                        <div class="barcode-container">
                            <svg id="barcode-<%= index %>" data-format="CODE128" data-value="<%= ticket.code %>" data-displayValue="false"></svg>
                        </div>
                        <div class="code-text"><%= ticket.code %></div>
                    </div>
                </div>
            <% }); %>

            <div class="nav-controls">
                <button id="prevBtn" class="nav-btn">←</button>
                <span id="counter" class="counter">1 / <%= receipt.tickets.length %></span>
                <button id="nextBtn" class="nav-btn">→</button>
            </div>
        </div>

        <div class="footer-actions">
            <button class="btn-print" onclick="window.print()" style="color: #ccc; border-color: #555;">Print Tickets</button>
            <% if (typeof fromLogVisit !== 'undefined' && fromLogVisit) { %>
                <a href="/visits/new" style="display:block; margin-top:15px; color:var(--ticket-gold); text-decoration:none; font-weight:bold;">Log Another Visit</a>
            <% } %>
        </div>

        <script>
            document.addEventListener('DOMContentLoaded', () => {
                const tickets = document.querySelectorAll('.ticket-visual');
                const prevBtn = document.getElementById('prevBtn');
                const nextBtn = document.getElementById('nextBtn');
                const counter = document.getElementById('counter');
                let currentIndex = 0;
                
                tickets.forEach((ticketEl, index) => {
                    const svg = ticketEl.querySelector('svg');
                    if(svg) JsBarcode(svg, svg.dataset.value, { format: "CODE128", displayValue: false, height: 70, width: 2, margin: 0 });
                });

                function updateUI() {
                    tickets.forEach((t, i) => t.classList.toggle('active', i === currentIndex));
                    counter.textContent = `${currentIndex + 1} / ${tickets.length}`;
                    prevBtn.disabled = currentIndex === 0;
                    nextBtn.disabled = currentIndex === tickets.length - 1;
                }
                
                if (prevBtn && nextBtn) {
                    prevBtn.addEventListener('click', () => { if (currentIndex > 0) { currentIndex--; updateUI(); } });
                    nextBtn.addEventListener('click', () => { if (currentIndex < tickets.length - 1) { currentIndex++; updateUI(); } });
                    updateUI();
                }
            });
        </script>

    <% } else { %>
        <div class="container">
            <div class="receipt-card">
                <div class="receipt-header">
                    <h1>Primus Park</h1>
                    <p>Visit Confirmation</p>
                </div>
                <div class="receipt-body">
                    <div class="info-row"><span class="label">Date</span><span class="value"><%= receipt.visit_date %></span></div>
                    <div class="info-row"><span class="label">Ticket Type</span><span class="value main"><%= receipt.ticket_name %></span></div>
                    
                    <% if (receipt.is_member) { %>
                        <div class="info-row"><span class="label">Member Name</span><span class="value"><%= receipt.member_name %></span></div>
                        <div class="info-row"><span class="label">Member ID</span><span class="value id"><%= (receipt.member_id || '').substring(0,8).toUpperCase() %></span></div>

                        <% if (receipt.ticket_name === 'Guest Pass') { %>
                            <div class="guest-pass-box">
                                <span class="guest-pass-text">Guest Passes Used</span>
                                <span class="guest-pass-val">1 Pass (Remaining: <%= receipt.guest_passes_remaining %>)</span>
                            </div>
                        <% } %>
                        
                        <% if (receipt.subMembers && receipt.subMembers.length > 0) { %>
                            <div class="sub-list">
                                <div style="font-weight:bold; font-size:0.8em; color:var(--gray); text-transform:uppercase; margin-bottom:10px;">Group Members Checked In</div>
                                <% receipt.subMembers.forEach(sub => { %>
                                    <div class="sub-item"><span><%= sub.first_name %> <%= sub.last_name %></span><span class="value id" style="font-size:0.9em;"><%= (sub.membership_id || '').substring(0,8).toUpperCase() %></span></div>
                                <% }); %>
                            </div>
                        <% } %>
                    <% } else { %>
                         <div class="info-row"><span class="label">Discount</span><span class="value"><%= receipt.promo_applied %></span></div>
                    <% } %>
                    
                    <div class="info-row" style="margin-top:15px; border-top: 1px solid #f0f0f0; padding-top: 15px;">
                        <span class="label">Logged By</span><span class="value" style="font-size:0.9em;"><%= receipt.staff_name %></span>
                    </div>
                </div>
                <div class="receipt-footer">
                    <div class="footer-row"><span class="footer-label">Total Cost</span><span class="footer-value">$<%= receipt.total_cost.toFixed(2) %></span></div>
                </div>
            </div>

            <div class="footer-actions">
                <button class="btn-print" onclick="window.print()">Print Receipt</button>

                <% if (typeof fromLogVisit !== 'undefined' && fromLogVisit) { %>
                    <a href="/visits/new" class="btn-close" style="background-color: var(--secondary); color: var(--dark);">Log Another Visit</a>
                
                <% } else if (typeof user !== 'undefined' && user) { %>
                    <a href="/members" class="btn-close" style="background-color: var(--primary);">Back to Members List</a>

                <% } else if (typeof member !== 'undefined' && member) { %>
                    <a href="/member/history" class="btn-close" style="background-color: var(--accent);">Back to My History</a>
                <% } %>
            </div>
        </div>
    <% } %>

</body>
</html>