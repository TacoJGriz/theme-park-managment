<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Request Restock</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        h1 { color: #444; text-align: center; margin-top: 1em; }
        h1 small { color: #007bff; font-weight: bold; display: block; font-size: 0.7em; }
        form { max-width: 500px; margin: 1em auto 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        div { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; }
        input { width: 100%; padding: 10px; box-sizing: border-box; border-radius: 4px; border: 1px solid #ccc; font-size: 1.1em; }
        input[type="number"]:disabled { background-color: #e9ecef; cursor: not-allowed; }
        .error { color: #d9534f; font-weight: bold; background-color: #f2dede; padding: 1em; border-radius: 4px; margin-bottom: 15px; }
        button { padding: 12px 20px; background-color: #007bff; color: white; border: none; cursor: pointer; border-radius: 4px; font-size: 1em; }
        button:hover { background-color: #0056b3; }
        .cancel-link { display: inline-block; margin-left: 15px; text-decoration: none; color: #6c757d; font-weight: bold; }
        .cancel-link:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <h1>Request Inventory Restock
        <small>For: <%= item.item_name %> at <%= item.vendor_name %></small>
    </h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <form action="/inventory/request/<%= item.vendor_id %>/<%= item.item_id %>" method="POST">
        <div>
            <label for="current_count">Current Count:</label>
            <input type="number" id="current_count" name="current_count" value="<%= item.current_count %>" disabled>
        </div>
        <div>
            <label for="requested_count">Amount to Add:</label>
            <input type="number" id="requested_count" name="requested_count" min="1" required>
        </div>
        <br>
        <button type="submit">Submit Request</button>
        <a href="/inventory" class="cancel-link">Cancel</a>
    </form>
</body>
</html>