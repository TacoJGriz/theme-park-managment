<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Maintenance History</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 1200px; margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        
        /* Header Styling */
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em; margin-bottom: 1em; display: flex; justify-content: space-between; align-items: center; }
        h1 small { font-size: 0.6em; color: #777; font-weight: normal; margin-left: 10px; }

        /* Buttons */
        .btn-report { background-color: #28a745; color: white; padding: 10px 20px; text-decoration: none; border-radius: 4px; font-weight: bold; font-size: 0.9em; }
        .btn-report:hover { background-color: #218838; }

        .back-link { display: inline-block; margin-bottom: 1.5em; text-decoration: none; color: #007bff; font-weight: bold; padding: 8px 15px; background-color: #e9ecef; border-radius: 4px; }
        .back-link:hover { background-color: #dee2e6; color: #0056b3; }

        /* Table Styling */
        table { width: 100%; border-collapse: collapse; margin-top: 1em; }
        th { background-color: #17a2b8; color: white; padding: 12px; text-align: left; }
        td { padding: 12px; border-bottom: 1px solid #ddd; vertical-align: top; }
        tr:nth-child(even) { background-color: #f9f9f9; }

        /* Status Badges */
        .status-badge { display: inline-block; padding: 4px 8px; border-radius: 4px; font-size: 0.85em; font-weight: bold; text-transform: uppercase; }
        .status-open { background-color: #ffeeba; color: #856404; }
        .status-complete { background-color: #d4edda; color: #155724; }
        .status-pending { background-color: #fff3cd; color: #856404; border: 1px solid #ffeeba; }

        /* Action Buttons */
        .btn-action { display: inline-block; padding: 5px 10px; font-size: 0.85em; font-weight: bold; text-decoration: none; border-radius: 4px; border: 1px solid transparent; margin-right: 5px; }
        
        .btn-complete { color: #28a745; border-color: #28a745; background: white; }
        .btn-complete:hover { background-color: #28a745; color: white; }

        .btn-assign { color: #ffc107; border-color: #ffc107; background: white; color: #333; }
        .btn-assign:hover { background-color: #ffc107; color: #333; }

        .cost { font-family: monospace; font-weight: bold; color: #555; }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        
        <%# --- SMART RECALL BUTTON --- %>
        <% if (typeof back_query !== 'undefined' && back_query) { %>
            <%# Decode the query string to ensure it renders correctly %>
            <a href="/reports/maintenance?<%= decodeURIComponent(back_query) %>" class="back-link">&laquo; Back to Maintenance Report</a>
        <% } else { %>
            <a href="/rides" class="back-link">&laquo; Back to Rides List</a>
        <% } %>

        <h1>
            Maintenance History
            <small>Ride: <%= ride.ride_name %></small>
            <a href="/maintenance/new/<%= ride.public_ride_id %>" class="btn-report">+ Report New Issue</a>
        </h1>

        <table>
            <thead>
                <tr>
                    <th>Report Date</th>
                    <th style="width: 30%;">Issue Summary</th>
                    <th>Assigned To</th>
                    <th>Completion</th>
                    <th>Cost</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (maintenance_logs.length === 0) { %>
                    <tr><td colspan="7" style="text-align: center; padding: 2em;">No maintenance records found.</td></tr>
                <% } %>

                <% maintenance_logs.forEach(log => { %>
                    <tr>
                        <td><%= new Date(log.report_date).toLocaleDateString() %></td>
                        <td><%= log.summary %></td>
                        <td>
                            <%= log.employee_name || 'Unassigned' %>
                            <% if (log.pending_employee_name) { %>
                                <br><small style="color: #e0a800;">(Pending transfer to: <%= log.pending_employee_name %>)</small>
                            <% } %>
                        </td>
                        <td><%= log.end_date ? new Date(log.end_date).toLocaleDateString() : '---' %></td>
                        <td>
                            <% if (log.cost) { %>
                                <span class="cost">$<%= parseFloat(log.cost).toFixed(2) %></span>
                            <% } else { %>
                                -
                            <% } %>
                        </td>
                        
                        <%# --- COLUMN 1: STATUS --- %>
                        <td>
                            <% if (log.end_date) { %>
                                <span class="status-badge status-complete">Complete</span>
                            <% } else { %>
                                <span class="status-badge status-open">Open</span>
                            <% } %>
                        </td>

                        <%# --- COLUMN 2: ACTIONS --- %>
                        <td>
                            <% if (!log.end_date) { %>
                                <a href="/maintenance/complete/<%= log.public_maintenance_id %>" class="btn-action btn-complete">Complete</a>
                                <a href="/maintenance/reassign/<%= log.public_maintenance_id %>" class="btn-action btn-assign">Reassign</a>
                            <% } else { %>
                                <span style="color: #aaa; font-size: 0.9em;">Closed</span>
                            <% } %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>