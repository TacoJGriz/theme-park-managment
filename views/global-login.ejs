<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login | Primus Park</title>
    <%# --- Import Google Fonts --- %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* --- STANDARDIZED THEME VARIABLES --- */
        :root {
            --primary: #6f42c1;       /* Magical Purple */
            --primary-dark: #5a32a3;  /* Deep Purple */
            --secondary: #ffc107;     /* Gold */
            --accent: #17a2b8;        /* Cyan/Teal */
            --dark: #343a40;          /* Charcoal */
            --light: #f8f9fa;         /* Off-white */
            --white: #ffffff;
            --gray: #6c757d;
            --danger: #dc3545;
            
            --shadow-lg: 0 10px 30px rgba(111, 66, 193, 0.3); /* Purple glow */
            --radius: 12px;
            --transition: all 0.3s ease;
            --hero-overlay: rgba(0, 0, 0, 0.4); 
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            background-color: var(--light);
            color: var(--dark);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            /* Hero Background */
            background: linear-gradient(var(--hero-overlay), var(--hero-overlay)), url('/images/hero-bg.png');
            background-size: cover;
            background-position: center 30%;
            background-attachment: fixed;
        }

        /* --- Standardized Card --- */
        form {
            max-width: 450px;
            width: 100%;
            background: rgba(255, 255, 255, 0.95); /* Glass effect */
            padding: 3em;
            border-radius: var(--radius);
            box-shadow: var(--shadow-lg);
            border-top: 5px solid var(--primary); /* Theme Accent */
            box-sizing: border-box;
        }

        h1 {
            font-family: 'Lobster', cursive;
            color: var(--dark);
            text-align: center;
            margin-top: 0;
            margin-bottom: 1.5em;
            border-bottom: 2px solid #eee;
            padding-bottom: 0.5em;
            letter-spacing: 1px;
            font-size: 2.5em;
        }

        div.form-group { margin-bottom: 1.5em; }
        
        label { display: block; margin-bottom: 8px; font-weight: 600; color: var(--dark); }

        /* --- Standardized Inputs --- */
        input[type="text"], input[type="email"], input[type="password"] {
            width: 100%;
            padding: 12px 15px;
            box-sizing: border-box;
            border-radius: 8px; 
            border: 2px solid #eee;
            font-family: 'Poppins', sans-serif;
            font-size: 1em;
            transition: var(--transition);
        }
        input:focus { 
            outline: none; 
            border-color: var(--accent);
            box-shadow: 0 0 0 4px rgba(23, 162, 184, 0.1);
        }

        .error {
            color: var(--white);
            font-weight: 600;
            background-color: var(--danger);
            padding: 1em;
            border-radius: 8px;
            margin-bottom: 1.5em;
            text-align: center;
            box-shadow: 0 4px 10px rgba(220, 53, 69, 0.2);
        }

        /* --- Standardized Button (.btn-primary) --- */
        button {
            width: 100%;
            padding: 12px 20px;
            background-color: var(--primary);
            color: var(--white);
            border: none;
            cursor: pointer;
            border-radius: 30px; /* Pill Shape */
            font-size: 1.1em;
            font-weight: 600;
            transition: var(--transition);
            box-shadow: 0 4px 10px rgba(111, 66, 193, 0.3);
        }
        button:hover { 
            background-color: var(--primary-dark); 
            transform: translateY(-2px);
        }
        
        .info-note {
            text-align: center;
            font-size: 0.9em;
            color: var(--gray);
            margin-top: 1.5em;
            margin-bottom: 0;
        }
        .info-note a {
            color: var(--accent);
            text-decoration: none;
            font-weight: 700;
            transition: color 0.2s;
        }
        .info-note a:hover { text-decoration: underline; color: var(--primary); }
    </style>
</head>
<body>
    
    <form action="/login" method="POST">
        <h1>Park Login</h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <div class="form-group">
            <label for="username">Email Address:</label>
            <input type="text" id="username" name="username" placeholder="Enter your email" required>
        </div>
        
        <div class="form-group">
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>
        </div>
        
        <button type="submit">Sign In</button>

        <p class="info-note">
            Bought your pass in the park? <br>
            <a href="/member/register">Activate your account here</a>
        </p>
         <p class="info-note" style="margin-top: 10px;">
            <a href="/">&laquo; Back to Homepage</a>
        </p>
    </form>
</body>
</html>