<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Pending Employee Approvals</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 900px; margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em; }
        table { width: 100%; border-collapse: collapse; margin-top: 1.5em; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #fd7e14; color: white; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .approve-button {
            background-color: #28a745; color: white; padding: 8px 12px; border: none; border-radius: 4px; cursor: pointer; font-weight: bold;
        }
        .approve-button:hover { background-color: #218838; }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h1>Pending Employee Approvals</h1>

        <% if (typeof success !== 'undefined' && success) { %>
            <p style="color: green; font-weight: bold;"><%= success %></p>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
            <p style="color: red; font-weight: bold;"><%= error %></p>
        <% } %>

        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Role to Add</th>
                    <th>Hire Date</th>
                    <th>Location</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (users.length === 0) { %>
                    <tr>
                        <td colspan="6" style="text-align: center;">There are no employees pending approval.</td>
                    </tr>
                <% } %>
                <% users.forEach(targetUser => { %>
                    <tr>
                        <td><%= targetUser.first_name %> <%= targetUser.last_name %></td>
                        <td><%= targetUser.email %></td>
                        <td><%= targetUser.employee_type %></td>
                        <td><%= targetUser.hire_date.toISOString().substring(0, 10) %></td>
                        <td><%= targetUser.location_name || 'N/A' %></td>
                        <td>
                            <%# UPDATED: Only Admin can approve now %>
                            <% if (user.role === 'Admin') { %>
                                <form action="/employees/approve/<%= targetUser.public_employee_id %>" method="POST" onsubmit="return confirm('Are you sure you want to approve this employee?');">
                                <button type="submit" class="approve-button">Approve</button>
                                </form>
                            <% } else { %>
                                <span>(Pending Approval)</span>
                            <% } %>
                        </td>
                   </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>